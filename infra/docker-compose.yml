services:
  yolo:
    build: 
      context: ../models/yolo    # this folder must contain Dockerfile, requirements.txt, app/
      dockerfile: Dockerfile
    container_name: yolo
    image: myproject/yolo:latest
    ports:
      - "8000:8000"
    environment:
      - YOLO_MODEL_PATH=yolov8n.pt
      - YOLO_DEVICE=cpu
      - YOLO_CONF_THRESHOLD=0.25
    volumes:
      - ./storage/uploads:/data/uploads
      - ./storage/annotated:/data/annotated

  backend:
    image: node:20
    container_name: backend
    working_dir: /usr/src/app
    environment:
      BACKEND_PORT: ${BACKEND_PORT}
      BACKEND_LOG_LEVEL: ${BACKEND_LOG_LEVEL}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_PORT: ${MYSQL_PORT}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3300:3300"
    volumes:
      - ../backend:/usr/src/app
      - ./storage/uploads:/usr/src/app/storage/uploads
    command: bash -lc "npm install && npm run dev"

  frontend:
    image: node:20
    container_name: frontend
    working_dir: /usr/src/app
    depends_on:
      - backend
    environment:
      FRONTEND_PORT: ${FRONTEND_PORT}
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
    ports:
      - "3000:3000"
    volumes:
      - ../frontend:/usr/src/app
    command: bash -lc "npm install && npm run dev"

volumes:
  mysql_data:

